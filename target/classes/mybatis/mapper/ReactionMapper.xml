<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eggmoney.payv.infrastructure.mybatis.mapper.ReactionMapper">

  <!-- 특정 유저가 특정 보드에 남긴 리액션 조회 -->
  <select id="selectByUserAndBoard" resultType="com.eggmoney.payv.infrastructure.mybatis.record.ReactionRecord">
    SELECT reaction_id AS reactionId,
           board_id    AS boardId,
           user_id     AS userId,
           type
    FROM reaction
    WHERE user_id = #{userId}
      AND board_id = #{boardId}
  </select>

  <!-- 게시글별 리액션 카운트 -->
  <select id="countByBoard" resultType="long">
    SELECT COUNT(*)
    FROM reaction
    WHERE board_id = #{boardId}
  </select>

  <!-- 리액션 추가 -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="reactionId">
    INSERT INTO reaction (reaction_id, board_id, user_id, type)
    VALUES (#{reactionId}, #{boardId}, #{userId}, #{type})
  </insert>

  <!-- 리액션 삭제 -->
  <delete id="delete">
    DELETE FROM reaction WHERE reaction_id = #{reactionId}
  </delete>

</mapper>
